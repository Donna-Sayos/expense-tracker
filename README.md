# expense-tracker
A single-page expense tracker built with vanilla JS, HTML, and CSS. It lets you add, delete, sort, and filter expenses with live totals, and uses localStorage to survive page refreshes. It also uses async/await to fetch live exchange rates. Built per the specs in assignment-02.pdf.
